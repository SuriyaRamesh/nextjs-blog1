// import { withAuth } from "@kinde-oss/kinde-auth-nextjs/middleware";
// export default function middleware(req) {
//     // this is the preferrable place to do anthentication checks
//     return withAuth(req);
// }

// export const config = {
//     matcher: ["/posts/:path*", "/posts/:id"],
// }
//     // matcher: ["/posts/:path*"], // this will match all the posts pages


// // this exported function will take a incoming request and response object

// // data access layer is the best practice for authentication